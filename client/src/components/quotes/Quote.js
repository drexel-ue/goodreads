import React from "react";
import { withRouter } from "react-router-dom";

const Quotes = (props) => (
    <div>
        <h2>Book quotes can be found here.</h2>
    </div>
);

export default withRouter(Quotes);