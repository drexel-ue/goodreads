import React from "react";
import { withRouter } from "react-router-dom";

const FriendRecommendations = (props) => (
    <div>
        <h2>Book recommendations from your friends go here.</h2>
    </div>
);

export default withRouter(FriendRecommendations);