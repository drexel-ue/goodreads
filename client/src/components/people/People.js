import React from "react";
import { withRouter } from "react-router-dom";

const People = (props) => (
    <div>
        <h2>Popular reviewers can be found here.</h2>
    </div>
);

export default withRouter(People);