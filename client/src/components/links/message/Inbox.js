import React from "react";
import { withRouter } from "react-router-dom";

const Inbox = (props) => (
    <div>
        <h2>Your messages can be found here.</h2>
    </div>
);

export default withRouter(Inbox);