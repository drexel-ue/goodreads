import React from "react";
import { withRouter } from "react-router-dom";

const NewReleases = (props) => (
    <div>
        <h2>New book releases can be found here.</h2>
    </div>
);

export default withRouter(NewReleases);