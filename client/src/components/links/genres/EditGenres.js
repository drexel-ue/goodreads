import React from "react";
import { withRouter } from "react-router-dom";

const EditGenres = (props) => (
    <div>
        <h2>Edit your favorite genres!</h2>
    </div>
);

export default withRouter(EditGenres);