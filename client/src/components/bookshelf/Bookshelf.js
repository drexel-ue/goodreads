import React from "react";
import { withRouter } from "react-router-dom";
import Register from '../session/Register';

const Bookshelf = (props) => (
    <div>
        <h2>Your books go here.</h2>
    </div>
);

export default withRouter(Bookshelf);